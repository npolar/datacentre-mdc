{
  "name": "@npolar/datacentre-mdc",
  "version": "0.0.1-pre",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@material/list": "^3",
    "@material/mwc-base": "0.9.1",
    "@material/mwc-button": "0.9.1",
    "@material/mwc-drawer": "0.9.1",
    "@material/mwc-fab": "0.9.1",
    "@material/mwc-icon": "0.9.1",
    "@material/mwc-icon-button": "0.9.1",
    "@material/mwc-icon-button-toggle": "0.9.1",
    "@material/mwc-textfield": "0.9.1",
    "@material/mwc-top-app-bar-fixed": "0.9.1",
    "@material/theme": "^3",
    "@material/touch-target": "^4",
    "lit-element": "2.2.1"
  },
  "browserslist": [
    "chrome 69"
  ],
  "devDependencies": {
    "autoprefixer": "^9",
    "eslint": "^6",
    "node-sass": "^4",
    "postcss-cli": "^6",
    "rollup": "^1.22",
    "rollup-plugin-live-server": "^1",
    "rollup-plugin-node-resolve": "^5"
  },
  "scripts": {
    "dev": "yarn prebuild && yarn rollup -c -w",
    "prepare": "yarn build-styles",
    "eslint": "eslint src",
    "pretest": "yarn eslint",
    "test": "yarn build",
    "prebuild": "mkdir -p dist && cp index.html dist/",
    "build": "yarn build-styles && yarn rollup -c",
    "build-styles": "./src/style/build-styles",
    "now": "yarn build && cp now.json dist/ && cd dist && now && cd -"
  },
  "files": [
    "dist",
    "src"
  ]
}
