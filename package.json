{
  "name": "@npolar/datacentre-mdc",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@material/list": "^3.2.0",
    "@material/mwc-button": "^0",
    "@material/mwc-drawer": "^0",
    "@material/mwc-fab": "^0.8.0",
    "@material/mwc-icon": "^0",
    "@material/mwc-icon-button": "^0.8.0",
    "@material/mwc-icon-button-toggle": "^0",
    "@material/mwc-textfield": "^0",
    "@material/mwc-top-app-bar": "^0.8.0",
    "lit-element": "^2.1"
  },
  "browserslist": [
    "chrome 69"
  ],
  "devDependencies": {
    "autoprefixer": "^9",
    "es-dev-server": "^1",
    "eslint": "^6",
    "node-sass": "^4",
    "postcss-cli": "^6",
    "rollup": "^1",
    "rollup-plugin-live-server": "*",
    "rollup-plugin-node-resolve": "^5"
  },
  "scripts": {
    "dev": "yarn prebuild && yarn rollup -c -w",
    "prepare": "yarn build-styles",
    "eslint": "eslint src",
    "pretest": "yarn eslint",
    "test": "yarn build",
    "prebuild": "mkdir -p dist && cp index.html dist/",
    "build": "yarn build-styles && yarn rollup -c",
    "build-styles": "./src/style/build-styles"
  },
  "files": [
    "dist"
  ]
}
